version: 2.1

orbs:
  wsorb: whitesource/whitesource-scan@21.6.3

workflows:
  test:
    jobs:
    - wsorb/scan:
        config_file_path:  "wss-unified-agent.config"
        commands_file_path: "install_commands.sh" 
